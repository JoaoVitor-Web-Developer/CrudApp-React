-----> Como executar o docker:

docker exec -it mymysql bash
mysql -u root --password
root
show databases;
use tasksdb;
select * from tasks;


-----> Executar client:

cd client

-npm run dev

-----> Executar server:

-npm run dev
